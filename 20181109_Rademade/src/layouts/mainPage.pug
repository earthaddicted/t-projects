extends main

append params
	- page = 'index'
	- pageTitle = 'Home'

block main

	+b('section').section.section-admin-table
		+e.wrapper
			+b-asideBlock(globals.data)

			.right-col
				+b-infoPanel()
				+b-main-content()
